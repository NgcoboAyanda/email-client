import React from "react";

import './EmailViewer.css';

const EmailViewer = ({email=null}) => {

    return (
        <div className="email-viewer">
            <div>
                this is the email viewer
            </div>
        </div>
    )
}

export default EmailViewer;